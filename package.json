{
  "name": "fitness-game-monorepo",
  "private": true,
  "workspaces": [
    "mobile-app",
    "backend-api",
    "ml"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend-api run dev",
    "build:backend": "npm --workspace backend-api run build",
    "start:backend": "npm --workspace backend-api run start",
    "dev:mobile": "npm --workspace mobile-app run start",
    "lint": "echo \"Add linters as needed\""
  }
}
